— You are missing multi-tenant isolation enforcement
Right now orgId is present everywhere but not enforced as a boundary.
You must add this rule:
REQUIRED:
Every site query must enforce:
WHERE org_id = session.orgId
Not optional.
Add:
Required middleware:
resolveOrgScope()
Must inject:
orgId
siteId
userRole
Then every service receives orgId as required parameter.
Add this rule to spec:
All site services must require orgId parameter.
No global queries allowed.
Without this:
One bug = org data leak.
That is an investor red flag.
2 — Missing caching layer (this WILL hurt performance)
Right now every compile does:
fetch blocks
fetch bindings
fetch adapters
render blocks
At 5k orgs this becomes expensive.
Add:
Required:
site_render_cache
Table:
site_render_cache

site_id
block_id
rendered_html
data_hash
rendered_at
Rule:
Only re-render blocks when:
data hash changes.
This reduces compile cost massively.
3 — Missing site compilation strategy
Right now:
compileSite() renders everything every time.
Wrong.
Must become:
Required compile modes:
full_compile
block_compile
page_compile
Rules:
Full compile:
generation
publish
theme change
Block compile:
event update
sponsor update
announcement update
Page compile:
manual edit
This reduces cost dramatically.
4 — Missing queue system (polling is wrong)
Polling every 30 min is OK early.
But your architecture already exceeded polling.
You must add:
Required:
job_queue table
Schema:
job_queue

id
job_type

org_id
site_id

payload_json

status
// pending
// processing
// completed
// failed

attempts

scheduled_at
started_at
completed_at

error_message
Then jobs:
compile_site
update_block
recompute_metrics
run_auto_update
import_site
generate_site
Then:
Scheduler only dequeues jobs.
No polling logic inside services.
This prevents scaling collapse.
5 — Missing observability (major SaaS red flag)
You currently have:
site_change_log
That is not observability.
Add:
Required:
site_system_logs
Schema:
site_system_logs

id

org_id
site_id

service
operation

severity
// info
// warning
// error

message

metadata_json

created_at
Log:
generation failures
adapter failures
render failures
auto update failures
import failures
Without this:
Debugging becomes impossible.
6 — Missing failure safety rules
You must add:
REQUIRED:
Renderer must never break site.
If block fails:
Return:
<!-- block failed -->
and continue.
Add rule:
Site must always compile even if one block fails.
Otherwise:
One event bug = broken homepage.
7 — Missing data versioning rule
You added:
generatedSpecJson
Good.
But missing:
Required:
site_versions
Schema:
site_versions

id

site_id

version_number

spec_json

theme_json

compiled_html

created_at
Then:
publish creates version.
This is investor-grade behavior.
8 — Missing AI safety rule (VERY important)
Right now AI generates content.
You must add:
Required constraint:
AI cannot overwrite:
mission
about
history
programs
Unless:
manual approval
Add rule:
AI may only generate identity content during initial generation.
Never during auto update.
This prevents AI drift.
9 — Missing permissions model for blocks
Right now:
lockLevel exists.
But missing:
editable_by_role
Add to:
site_blocks:
editableByRoles text[]
Example:
owner
admin
content
events
finance
Future proofing.
10 — Missing Stripe Connect safety rule
You asked earlier.
Add this explicitly:
Renderer must NEVER show:
transaction ids
payer emails
internal revenue data
fees
refunds
Only:
tickets sold
tickets remaining
price
Nothing financial beyond that.
Add rule:
Adapters must sanitize financial data.
Now the biggest architectural improvement you should add
This is the one I would add if this were my product:
Organization Activity Engine
Instead of:
Auto update scanning everything.
Use:
Event-driven updates.
Add:
org_activity_stream
Schema:
org_activity_stream

id

org_id

activity_type
// event_created
// ticket_sold
// sponsor_added
// vendor_added
// announcement_posted

reference_id

processed

created_at
Then:
Activity triggers:
update jobs.
This is how real SaaS operates.
Not polling.
What I would change structurally (the one real improvement)
You are still thinking:
website builder.
You should think:
Organization Operating System with public interface.
Meaning:
Website is just a projection of operational data.
Your architecture is already moving there.
Lean fully into that.
One thing I would remove
Remove:
generatedSpecJson
Or move to:
site_versions.
Otherwise becomes junk storage.
One thing I would simplify
You do NOT need:
sourceTable column in site_data_sources
sourceType is enough.
Adapters know tables.
This prevents leaking DB structure.
Production readiness checklist you should add
Add this section to doc:
Production readiness requirements
Must have before paying customers:
Rate limiting
Org isolation enforcement
Error logging
Job queue
Data adapters
Site versions
Block failure safety
Metrics caching
Stripe webhook retry handling
Audit logs
Without these:
Not production SaaS