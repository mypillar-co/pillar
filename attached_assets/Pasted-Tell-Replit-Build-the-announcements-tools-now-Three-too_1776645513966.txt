Tell Replit:
Build the announcements tools now. Three tools: list_announcements, post_announcement, delete_announcement.
STEP 1 — Database migration. In the API server runMigrations() function add:
await db.execute(sqlCREATE TABLE IF NOT EXISTS cs_announcements (id SERIAL PRIMARY KEY, org_id TEXT NOT NULL, title TEXT NOT NULL, body TEXT NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()));
await db.execute(sqlCREATE INDEX IF NOT EXISTS cs_announcements_org_idx ON cs_announcements(org_id));
Restart the API server and confirm the table exists before doing anything else.
STEP 2 — Add the three tools to the tools array in management.ts:
{ name: “list_announcements”, description: “Show all announcements posted to the members portal. Use when the user says ‘what announcements are posted’, ‘show portal announcements’, ‘what did we post to members’, ‘any notices up’.”, input_schema: { type: “object”, properties: {}, required: [] } }
{ name: “post_announcement”, description: “Post a new announcement to the members-only portal. All logged-in members see it when they visit the portal. Use when the user says ‘post an announcement’, ‘tell the members something’, ‘put a notice on the portal’, ‘let members know about the meeting change’. Do NOT use for public site posts or for sending email — announcements are in-portal only.”, input_schema: { type: “object”, properties: { title: { type: “string”, description: “Announcement title” }, body: { type: “string”, description: “Announcement body text” } }, required: [“title”, “body”] } }
{ name: “delete_announcement”, description: “Remove an announcement from the members portal permanently. Use when the user says ‘delete that announcement’, ‘take down the old notice’, ‘remove it from the portal’. Always confirm before deleting.”, input_schema: { type: “object”, properties: { announcementId: { type: “string”, description: “The ID of the announcement to remove” } }, required: [“announcementId”] } }
STEP 3 — Add to the dispatcher switch:
case “list_announcements”: { const rows = await db.execute(sqlSELECT id, title, body, created_at FROM cs_announcements WHERE org_id = ${org.id} ORDER BY created_at DESC LIMIT 20); result = { announcements: rows.rows, count: rows.rows.length }; break; }
case “post_announcement”: { const { title, body } = input as { title: string; body: string }; const rows = await db.execute(sqlINSERT INTO cs_announcements (org_id, title, body) VALUES (${org.id}, ${title}, ${body}) RETURNING id, title, created\_at); result = { ok: true, announcement: rows.rows[0], message: Announcement "${title}" posted to the members portal }; break; }
case “delete_announcement”: { const { announcementId } = input as { announcementId: string }; const existing = await db.execute(sqlSELECT id FROM cs_announcements WHERE id = ${announcementId} AND org_id = ${org.id}); if (!existing.rows[0]) { result = { ok: false, error: “Announcement not found” }; break; } await db.execute(sqlDELETE FROM cs_announcements WHERE id = ${announcementId} AND org_id = ${org.id}); result = { ok: true, message: “Announcement removed from the members portal” }; break; }
STEP 4 — Confirm the api-server builds clean and restart. Then test with these three requests and report what the agent does:
Test 1: “Post an announcement that our June meeting is moved to June 15th at 7pm”
Test 2: “What announcements do we have up right now”
Test 3: “Delete announcement number 1”
When those three pass, move to reply_to_message without waiting for further instructions. That tool sends an email reply to a contact form message. It reads the sender_email from the cs_messages table for the given messageId, calls Resend with the org’s senderEmail as the from address, and marks the message as replied by setting replied_at on the row. Come back when both announcements and reply_to_message are done.​​​​​​​​​​​​​​​​