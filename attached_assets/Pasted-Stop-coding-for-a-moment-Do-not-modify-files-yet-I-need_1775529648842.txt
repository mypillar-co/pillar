Stop coding for a moment. Do not modify files yet.

I need a precise diagnostic of why this project is not generating/rendering a dynamic Pillar tenant site after the interview completes.

Context:
- This must live on Pillar, not Discover Irwin.
- Each organization should exist as a tenant on a subdomain like orgslug.mypillar.co.
- We are NOT creating a brand-new standalone React/Vite project per org.
- We ARE creating a new organization inside the Pillar platform and rendering its public website from config/data.
- The interview already collects answers and saves them.
- The missing part is that the tenant site is not being built/rendered correctly after answers are saved.

Your task right now is ONLY to inspect the current codebase and report facts.

Please answer these questions one by one with file names, function names, and code paths:

1. Where exactly are the interview answers stored after submission?
   - Table name / schema
   - API route
   - server function
   - frontend submission function

2. After interview completion, what exact function is supposed to run next?
   - Name of function
   - Where it is called
   - Whether it actually runs
   - What data it receives

3. Is there currently any function that transforms interview answers into a normalized organization/site config?
   - If yes, show the function name and returned object shape
   - If no, say clearly: “No answer-to-config transform currently exists”

4. Is there currently any function that creates a new organization/tenant record after interview completion?
   - If yes, show where
   - If no, say clearly: “No tenant provisioning step currently exists”

5. Is there currently any subdomain or hostname-based org resolution in the app?
   - Example: reading req.hostname, extracting slug, loading org by slug/domain
   - If yes, show middleware/function
   - If no, say clearly: “No hostname-based tenant resolution currently exists”

6. What does the public website currently render from?
   - Hardcoded Discover Irwin content?
   - A single org config table?
   - CMS content?
   - Environment variables?
   - Static frontend constants?
   Please point to exact files/components.

7. Is the homepage actually configuration-driven today?
   - Which components are dynamic?
   - Which sections are hardcoded?
   - Is there a section registry?
   - Is there a page schema?
   Be specific.

8. When a new interview is completed, what observable artifact is created right now?
   - Just saved answers?
   - A config row?
   - Seeded events?
   - A slug?
   - Nothing public-facing?
   Explain exactly what happens and what does NOT happen.

9. Is there any setup/provisioning API already present?
   - For example POST /api/pillar/setup
   - If yes, is it wired into the interview flow?
   - If not wired, explain the disconnect clearly.

10. What is the single biggest blocker preventing:
   interview answers → org config → org tenant → rendered public site
   Choose only one primary blocker and justify it from the code.

11. Give me a simple step-by-step trace of the current flow as implemented today:
   user starts interview →
   user submits answer →
   data saved to ___ →
   next function ___ →
   final rendered result ___

12. Then give me a “missing pipeline” section:
   These are the exact missing steps between saved interview answers and a live orgslug.mypillar.co site.

Important rules:
- Do not give vague advice.
- Do not propose a rebuild yet.
- Do not say “could” or “might” unless absolutely necessary.
- Base your answer on the actual current code only.
- Quote the relevant files and function names.
- End with:
  A) What currently works
  B) What is missing
  C) What should be built next first