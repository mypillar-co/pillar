Proceed to upgrade specs 22–24 in one pass using the validated spec 21 pattern.

Do not modify application code. Only update tests and helpers.

Objective

Make specs 22–24 deterministic and aligned with real product flows:

* exact selectors (no fuzzy matching)
* no static waits
* poll DB or API for truth
* handle any proposal→confirm flows if present

⸻

Step 1 — extend helpers

In artifacts/e2e/helpers.ts, ensure these exist:

export async function getSiteConfig(slug: string) {
  const res = await dbQuery(`
    SELECT site_config
    FROM organizations
    WHERE slug = $1
  `, [slug]);
  return res?.[0]?.site_config;
}
export async function waitForSiteConfigChange(
  slug: string,
  predicate: (cfg: any) => boolean,
  timeout = 60000
) {
  await expect.poll(async () => {
    const cfg = await getSiteConfig(slug);
    return predicate(cfg);
  }, { timeout }).toBeTruthy();
}

Keep applyAndLaunch(page) as implemented.

⸻

Step 2 — spec 22 (members)

File: artifacts/e2e/22-member-complete-flow.spec.ts

Changes:

1. Replace all fuzzy selectors with exact role/name or type selectors.

await page.getByRole("button", { name: "Add Member" }).click();
const dialog = page.getByRole("dialog", { name: "Add Member" });
await dialog.locator('input[type="text"]').first().fill("Playwright");
await dialog.locator('input[type="email"]').fill("pw.member@example.com");
await dialog.locator('input[type="date"]').first().fill("2026-04-23");
await dialog.getByRole("button", { name: "Save" }).click();

2. Remove any waitForTimeout.
3. Validate via API (no DB schema guessing):

await expect.poll(async () => {
  const res = await page.request.get(`${API}/api/members`);
  if (!res.ok()) return false;
  const json = await res.json();
  return json?.some((m: any) => m.email === "pw.member@example.com");
}, { timeout: 30000 }).toBeTruthy();

4. If invite/register flow exists, split into a separate test; do not block the core “Add Member” test.

⸻

Step 3 — spec 23 (events)

File: artifacts/e2e/23-events-complete-flow.spec.ts

1. Use exact opener:

await page.getByTestId("events-new-event").click();

2. Fill required fields (use labels or types, no guessing).
3. Submit:

await page.getByRole("button", { name: "Create Event" }).click();

4. Replace waits with API verification:

await expect.poll(async () => {
  const res = await page.request.get(`${API}/api/events`);
  if (!res.ok()) return false;
  const json = await res.json();
  return json?.some((e: any) => e.name?.includes("Playwright"));
}, { timeout: 30000 }).toBeTruthy();

5. Optionally verify public page:

await page.goto(`${CP}/sites/${TEST_ORG_SLUG}/events`);
await expect(page.getByText(/Playwright/i)).toBeVisible();

⸻

Step 4 — spec 24 (content studio)

File: artifacts/e2e/24-content-studio-complete.spec.ts

1. Use exact task:

await page.getByRole("button", { name: "Press Release" }).click();

2. Trigger generation:

await page.getByRole("button", { name: "Generate" }).click();

3. Wait for output (no static waits):

const output = page.locator('[data-testid="content-output"], .output-panel');
await expect(output).not.toContainText("Output will appear here", { timeout: 30000 });

4. If a confirm/persist step exists, mirror spec 21:

await applyAndLaunch(page);

Only add this if network trace shows a second “provision/save” endpoint.

⸻

Step 5 — remove all static waits

Globally replace:

waitForTimeout(...)

with:

* expect(...).toBeVisible()
* expect.poll(...)
* API-based validation

⸻

Step 6 — keep request tracing scoped

For each spec, add temporary tracing only if needed:

page.on("request", r => {
  if (r.url().includes("/api/")) console.log("[REQ]", r.method(), r.url());
});
page.on("response", async r => {
  if (r.url().includes("/api/")) console.log("[RES]", r.status(), r.url());
});

Remove after stabilization.

⸻

Step 7 — run all three specs

timeout 90 pnpm exec playwright test \
  --config=artifacts/playwright.config.ts \
  --project=desktop-chrome \
  artifacts/e2e/22-member-complete-flow.spec.ts \
  artifacts/e2e/23-events-complete-flow.spec.ts \
  artifacts/e2e/24-content-studio-complete.spec.ts \
  --workers=1

⸻

Step 8 — report

Return only:

1. Pass/fail for specs 22, 23, 24
2. For each failure:
    * endpoint(s) hit
    * response status
    * whether issue is:
        * selector
        * missing confirmation step
        * API failure
        * wrong assertion target

Do not modify application code. Do not expand scope beyond these three specs.