Tell Replit:
Task 8 Part C only. Do nothing else.
Create artifacts/api-server/src/routes/members.ts with this content:
import { Router } from “express”;
import { db, membersTable } from “@workspace/db”;
import { eq, and, or, ilike, desc } from “drizzle-orm”;
import { resolveFullOrg } from “../lib/resolveOrg”;
const router = Router();
const VALID_TYPES = new Set([“general”, “board”, “honorary”, “staff”, “volunteer”]);
const VALID_STATUSES = new Set([“active”, “inactive”, “pending”]);
router.get(”/”, async (req, res) => {
const org = await resolveFullOrg(req, res);
if (!org) return;
const { status, type, search } = req.query as { status?: string; type?: string; search?: string };
const conditions = [eq(membersTable.orgId, org.id)];
if (status && VALID_STATUSES.has(status)) conditions.push(eq(membersTable.status, status));
if (type && VALID_TYPES.has(type)) conditions.push(eq(membersTable.memberType, type));
if (search) {
conditions.push(or(ilike(membersTable.firstName, %${search}%), ilike(membersTable.lastName, %${search}%), ilike(membersTable.email, %${search}%))!);
}
const members = await db.select().from(membersTable).where(and(…conditions)).orderBy(desc(membersTable.createdAt));
res.json(members);
});
router.post(”/”, async (req, res) => {
const org = await resolveFullOrg(req, res);
if (!org) return;
const { firstName, lastName, email, phone, memberType, status, joinDate, renewalDate, notes } = req.body;
if (!firstName) { res.status(400).json({ error: “firstName is required” }); return; }
if (memberType && !VALID_TYPES.has(memberType)) { res.status(400).json({ error: memberType must be one of: ${[...VALID_TYPES].join(", ")} }); return; }
if (status && !VALID_STATUSES.has(status)) { res.status(400).json({ error: status must be one of: ${[...VALID_STATUSES].join(", ")} }); return; }
const [member] = await db.insert(membersTable).values({ id: crypto.randomUUID(), orgId: org.id, firstName, lastName: lastName ?? null, email: email ?? null, phone: phone ?? null, memberType: memberType ?? “general”, status: status ?? “active”, joinDate: joinDate ?? null, renewalDate: renewalDate ?? null, notes: notes ?? null }).returning();
res.status(201).json(member);
});
router.put(”/:id”, async (req, res) => {
const org = await resolveFullOrg(req, res);
if (!org) return;
const [existing] = await db.select().from(membersTable).where(and(eq(membersTable.id, req.params.id), eq(membersTable.orgId, org.id)));
if (!existing) { res.status(404).json({ error: “Member not found” }); return; }
const { firstName, lastName, email, phone, memberType, status, joinDate, renewalDate, notes } = req.body;
if (memberType && !VALID_TYPES.has(memberType)) { res.status(400).json({ error: memberType must be one of: ${[...VALID_TYPES].join(", ")} }); return; }
if (status && !VALID_STATUSES.has(status)) { res.status(400).json({ error: status must be one of: ${[...VALID_STATUSES].join(", ")} }); return; }
const [updated] = await db.update(membersTable).set({ firstName: firstName ?? existing.firstName, lastName: lastName !== undefined ? lastName : existing.lastName, email: email !== undefined ? email : existing.email, phone: phone !== undefined ? phone : existing.phone, memberType: memberType ?? existing.memberType, status: status ?? existing.status, joinDate: joinDate !== undefined ? joinDate : existing.joinDate, renewalDate: renewalDate !== undefined ? renewalDate : existing.renewalDate, notes: notes !== undefined ? notes : existing.notes, updatedAt: new Date() }).where(eq(membersTable.id, req.params.id)).returning();
res.json(updated);
});
router.delete(”/:id”, async (req, res) => {
const org = await resolveFullOrg(req, res);
if (!org) return;
const [existing] = await db.select().from(membersTable).where(and(eq(membersTable.id, req.params.id), eq(membersTable.orgId, org.id)));
if (!existing) { res.status(404).json({ error: “Member not found” }); return; }
await db.delete(membersTable).where(eq(membersTable.id, req.params.id));
res.status(204).send();
});
router.get(”/export/csv”, async (req, res) => {
const org = await resolveFullOrg(req, res);
if (!org) return;
const members = await db.select().from(membersTable).where(eq(membersTable.orgId, org.id)).orderBy(membersTable.lastName, membersTable.firstName);
const rows = [[“First Name”,“Last Name”,“Email”,“Phone”,“Type”,“Status”,“Join Date”,“Renewal Date”,“Notes”], …members.map(m => [m.firstName, m.lastName ?? “”, m.email ?? “”, m.phone ?? “”, m.memberType, m.status, m.joinDate ?? “”, m.renewalDate ?? “”, m.notes ?? “”])];
const csv = rows.map(r => r.map(cell => "${String(cell).replace(/"/g, '""')}").join(”,”)).join(”\n”);
res.setHeader(“Content-Type”, “text/csv”);
res.setHeader(“Content-Disposition”, attachment; filename="members-${Date.now()}.csv");
res.send(csv);
});
router.get(”/stats”, async (req, res) => {
const org = await resolveFullOrg(req, res);
if (!org) return;
const members = await db.select({ status: membersTable.status, memberType: membersTable.memberType }).from(membersTable).where(eq(membersTable.orgId, org.id));
res.json({ total: members.length, active: members.filter(m => m.status === “active”).length, board: members.filter(m => m.memberType === “board”).length, pending: members.filter(m => m.status === “pending”).length });
});
export default router;
Then open artifacts/api-server/src/routes/index.ts and add these two lines in the correct location alongside the other route registrations:
import membersRouter from “./members”;
router.use(”/members”, membersRouter);
Restart the API server. Confirm GET /api/members returns an empty array and GET /api/members/stats returns { total: 0, active: 0, board: 0, pending: 0 } before doing anything else.
Come back when Part C is confirmed and I’ll give you Part D.​​​​​​​​​​​​​​​​