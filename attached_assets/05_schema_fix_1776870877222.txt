Create Drizzle config:

drizzle.config.ts

Add missing tables:
- cs_announcements

Create schema index file

Add scripts:
- db:generate
- db:migrate
- db:push

Remove bootstrap SQL execution from server startup
