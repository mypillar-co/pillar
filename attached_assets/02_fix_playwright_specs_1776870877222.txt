Update specs 21–26:

- Use correct routes:
  /dashboard/site
  /dashboard/members
  /dashboard/events
  /dashboard/content
  /dashboard/autopilot

- Replace all text-based selectors with getByTestId()

- Remove fuzzy matches like:
  "Create", "Add", "New"

- Use exact selectors only
