Good. Finish the current implementation pass first.

Then do NOT add more architecture or more tables.

Your next objective is output quality and correctness for 3 specific flows only:

1. Event-driven org
2. Membership-driven org
3. Import existing site

Focus on making those 3 flows excellent, not more generic.

PRIORITY 1 — FIX CORRECTNESS GAPS

Fix these first:

1. Real signal counts in generate route
- eventCount must come from real public event rows
- sponsorCount must come from real visible sponsor rows
- programCount must come from real program/import/interview data
- do not use placeholders or hardcoded 0s

2. Missing schema fields that block correct public rendering
Add and wire fully:
- events.show_on_public_site
- events.featured_on_site
- sponsors.tier_rank
- sponsors.site_visible

3. Auto-update must actually update the site
runAutoUpdate must:
- refresh adapter data
- re-render affected dynamic blocks
- call compileSite
- update generatedHtml
- write site_change_log

4. Import findings must affect strategy
Import data must influence:
- eventCount
- missionStrength
- imageRichness
- membershipPresence
- CTA priority
- layout strategy

Do not let import exist as passive findings only.

PRIORITY 2 — MAKE THE 3 CORE FLOWS GREAT

A. Event-driven org
Make this feel excellent:
- featured event hero actually works
- event list is clean and visually strong
- sponsor strip tiers correctly
- CTA logic uses real event behavior
- public site updates when dashboard event/sponsor data changes

B. Membership-driven org
Make this feel warm and clear:
- strong mission hero
- benefits/join section with specific content
- event presence secondary
- obvious join/contact flow
- avoid generic filler language

C. Import existing site
Make this feel magical:
- preserve logo
- preserve strong mission text verbatim
- infer real structure from source site
- rebuild into a cleaner, better site
- do not scramble or genericize the imported identity

PRIORITY 3 — SHOW ME PROOF

After fixing the above, show me:

1. One real event-driven generated site
2. One real membership-driven generated site
3. One imported site before/after
4. The exact strategy chosen for each
5. The exact signals used for each
6. Which blocks were dynamic vs generated
7. Which remaining weaknesses still exist

IMPORTANT CONSTRAINTS

- Do not add more architecture
- Do not add more tables unless absolutely required to fix correctness
- Do not broaden scope
- Do not optimize for hypothetical future org types
- Do not generalize further

The goal now is not platform completeness.
The goal now is:
make the outputs for these 3 flows genuinely good.