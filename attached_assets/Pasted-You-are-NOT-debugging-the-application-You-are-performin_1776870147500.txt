You are NOT debugging the application.

You are performing a **pure selector alignment pass** across Playwright specs 21–26 using the verified UI selectors below.

## Objective

Fix all failing specs caused by incorrect selectors.
DO NOT change application code.
DO NOT change logic.
ONLY update selectors and routes.

---

## GLOBAL RULES

1. Prefer stable selectors in this order:

   * data-tour / data-testid
   * exact button text (case-sensitive)
   * input[type=...] where applicable
   * scoped selectors within dialogs

2. Never use fuzzy text like:

   * "Create"
   * "Add"
   * "New"
     unless exact match is confirmed

3. Always navigate to the correct route before querying.

---

# REQUIRED FIXES

---

## SPEC 21 — AI EDIT

❌ CURRENT ISSUE
Wrong route: `/dashboard/website`

✅ FIX

* Navigate to: `/dashboard/site`
* Target textarea using placeholder:

```ts
await page.locator('textarea[placeholder*="Change our primary color"]')
```

This is the ONLY valid selector.

---

## SPEC 22 — MEMBER FLOW

### Open dialog

```ts
await page.click('button:has-text("Add Member")')
```

### Fill fields (scope to dialog)

```ts
const dialog = page.locator('div[role="dialog"]:has-text("Add Member")')

await dialog.locator('input[type="email"]').fill('test@example.com')
await dialog.locator('input[type="date"]').first().fill('2026-01-01')
```

### Submit

```ts
await dialog.locator('button:has-text("Save")').click()
```

⚠️ DO NOT look for "Create" or "Add" — it does not exist.

---

## SPEC 23 — EVENT FLOW

### Open dialog

```ts
await page.click('[data-tour="new-event-btn"]')
```

### Submit

```ts
await page.click('button:has-text("Create Event")')
```

⚠️ "Create" alone is invalid — must be exact.

---

## SPEC 24 — CONTENT STUDIO

### Select task

```ts
await page.click('button:has-text("Press Release")')
```

### Generate

```ts
await page.click('button:has-text("Generate")')
```

### Wait for output

```ts
await expect(page.locator('text=Output will appear here')).not.toBeVisible()
```

---

## SPEC 25 — AUTOPILOT

❌ CURRENT ISSUE

* Wrong route
* Looking for textarea
* Wrong placeholder heuristic

✅ FIX

### Navigate

```ts
await page.goto('/dashboard/autopilot')
```

### Locate input

```ts
const input = page.locator('input[placeholder^="Try:"]')
```

### Fill

```ts
await input.fill('How many members are there?')
```

---

## SPEC 26 — HERO IMAGE

### Navigate

```ts
await page.goto('/dashboard/site')
```

### Wait for panel

```ts
await page.waitForSelector('button:has-text("Upload photo")')
```

### Buttons

```ts
const uploadBtn = page.locator('button:has-text("Upload photo")')
const aiBtn = page.locator('button:has-text("AI picks")')
```

⚠️ Buttons may be:

* hidden if phase === "approving"
* disabled if not idle

Handle with:

```ts
await expect(uploadBtn).toBeVisible()
```

---

# AFTER FIXES

1. Re-run ONLY specs 20–27:

```bash
timeout 90 pnpm exec playwright test artifacts/e2e/20-*.spec.ts --workers=1
```

2. Report:

   * Pass count
   * Fail count
   * Any REAL product failures (not selector issues)

---

# DO NOT

* Do not refactor tests
* Do not add new tests
* Do not modify UI code
* Do not guess selectors

---

# GOAL

Convert all selector-related failures into:

* PASS
  or
* TRUE PRODUCT FAILURES

Nothing else.

Proceed.
