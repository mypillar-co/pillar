That was a big find — a silent catch swallowing a JOIN typo meant every single org was seeing the welcome screen on return visits. That’s been broken since day one and nobody knew because there was no error logging.
Now for the preview enhancement. Tell Replit:
In artifacts/steward/src/pages/dashboard/CommunityBuilder.tsx, in the SiteManagementView component, add a site preview iframe above the AI edit form. The preview should show the live community site in an embedded frame so the org admin can see their site without leaving Steward.
Add this block at the very top of the SiteManagementView return, before the live site card:
{status.url && (
<div className="rounded-xl overflow-hidden border border-white/10 bg-[#0f1a2e]">
    <div className="flex items-center justify-between px-3 py-2 border-b border-white/10">
      <span className="text-xs text-slate-400 font-medium">Site Preview</span>
      <a href={status.url} target="_blank" rel="noopener noreferrer" className="text-xs text-[#d4a017] hover:text-[#b88a14] flex items-center gap-1">
        Open full site <ExternalLink className="w-3 h-3" />
      </a>
    </div>
    <div className="relative w-full" style={{ paddingBottom: "56.25%" }}>
      <iframe
        src={status.url}
        className="absolute inset-0 w-full h-full border-0"
        style={{ transform: "scale(0.75)", transformOrigin: "top left", width: "133.33%", height: "133.33%" }}
        title="Site preview"
        sandbox="allow-scripts allow-same-origin"
      />
    </div>
  </div>
)}Make sure ExternalLink is already imported from lucide-react in this file — it should already be there. Do not change anything else in SiteManagementView. Confirm the Steward builds without TypeScript errors.
Once that’s confirmed the management view will show a live scaled-down preview of the org’s site with an “Open full site” link, then the config summary, then the banner picker, then the AI edit form below. That’s a complete site management experience.​​​​​​​​​​​​​​​​